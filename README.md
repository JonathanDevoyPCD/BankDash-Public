# BankDash

BankDash is a browser-based personal finance dashboard.

This public repository contains only the sanitized application shell. It does not include private bank statements, generated transaction exports, or local environment secrets.

User financial data is intended to be stored in Supabase behind Row Level Security, or in the browser's local storage for manual checklist values.
